pipeline {
    agent any  // Executa em qualquer agente disponível
    stages {
        stage('Test') {
            steps {
                echo 'Este é um teste simples'
            }
        }
    }
}
